plugins {
    id 'com.android.application' version '8.5.0' apply false
    id 'org.jetbrains.kotlin.android' version '1.9.20' apply false
}

// ❌ 删除 allprojects { repositories { ... } } 整个块！

task clean(type: Delete) {
    delete rootProject.buildDir
}
