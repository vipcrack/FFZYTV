buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.5.0' // AGP 8.5.0 → Requires Gradle 8.7
    }
}
// ⚠️ 注意：allprojects.repositories 已移除，符合 Gradle 8.7 策略
