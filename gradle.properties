# Project-wide Gradle settings.

# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.

# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
# Default value: -Xmx1024m -XX:MaxMetaspaceSize=256m
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8

# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. More details, visit
# http://www.gradle.org/docs/current/userguide/multi_project_builds.html#sec:parallel
org.gradle.parallel=true

# Enables caching of build artifacts across builds.
org.gradle.caching=true

# Enable configuration on demand for faster builds.
# (Note: Deprecated in newer Gradle versions; may be removed in future)
# org.gradle.configureondemand=true

# AndroidX migration
android.useAndroidX=true
android.enableJetifier=true

# Kotlin options (if using Kotlin)
kotlin.code.style=official

# Increase Kotlin compilation memory if needed
# kotlin.daemon.jvm.options=-Xmx1024m

# Optional: Enable R8 full mode (for release builds)
android.enableR8.fullMode=true